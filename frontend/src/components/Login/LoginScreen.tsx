import React from 'react'
import styled from "styled-components";

const LoginScreen = () => {
  return (
    <LoginScreenStyles>LoginScreen</LoginScreenStyles>
  )
}

export default LoginScreen

const LoginScreenStyles = styled.div ``