import React from "react";
import styled from "styled-components";

const Home = () => {
    return <>Home</>;
};

export default Home;

const NavContainer = styled.nav`
    .navbar {
        display: none;
    }
`;
